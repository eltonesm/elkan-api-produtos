server:
  port: 8082

spring:
  application:
    name: elkan-api-produtos

  data:
    mongodb:
      uri: mongodb://localhost:27017/elkan_produtos_db
      auto-index-creation: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

